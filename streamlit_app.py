import streamlit 

streamlit.title('Too many cooks')

streamlit.header('Too many cooks')
streamlit.text('Too many cooks')
streamlit.text('Too many cooks')
streamlit.text('Too many cooks')
streamlit.text('Too many cooks')
                
